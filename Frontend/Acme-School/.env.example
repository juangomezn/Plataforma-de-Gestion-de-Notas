# Copia como .env.local o .env (Vite las carga automáticamente).
# Prefijo obligatorio: VITE_

# Base URL del API (opcional). En desarrollo con proxy de Vite no hace falta (se usa /api).
# En producción sin proxy, URL absoluta del backend, por ejemplo:
# VITE_API_URL=https://api.tu-dominio.com
